package org.evosuite.regression;
/*
 *  * @ a: all measures
	 * 
	 * @ s: state difference
	 * 
	 * @ b: branch distance
	 * 
	 * @ c: coverage
	 * 
	 * @ o: coverage old
	 * 
	 * @ n: coverage new
 */
public enum RegressionMeasure {
	ALL_MEASURES,
	STATE_DIFFERENCE,
	BRANCH_DISTANCE,
	COVERAGE,
	COVERAGE_OLD,
	COVERAGE_NEW	 
}
